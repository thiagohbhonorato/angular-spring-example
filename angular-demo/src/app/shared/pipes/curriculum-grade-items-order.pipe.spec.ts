import { CurriculumGradeItemsOrderPipe } from './curriculum-grade-items-order.pipe';

describe('CurriculumGradeItemsOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new CurriculumGradeItemsOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
